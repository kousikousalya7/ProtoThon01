a=int(input("Enter the number of rows"))
b=[1]
for i in range(a):
    print("\n")
    for x in b:
        print(x,end=" ")
        
    c=[]
    c.append(b[0])
    for j in range(len(b)-1):
        c.append(b[j]+b[j+1])
    c.append(b[-1])
    b=c
